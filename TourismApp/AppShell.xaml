<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TourismApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TourismApp">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Customer"
        ContentTemplate="{DataTemplate local:CustomerHomePage}"
        Route="CustomerHomePage" />

    <ShellContent
        Title="Restaurant"
        ContentTemplate="{DataTemplate local:RestaurantHomePage}"
        Route="RestaurantHomePage" />

</Shell>